export default class User {
    constructor(name, firstname, role, email, password) {
      this.name = name;
      this.firstname = firstname;
      this.role = role;
      this.email = email;
      this.password = password;
    }

  }
